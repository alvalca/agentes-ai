# ── requirements.txt — Backend ────────────────────────────────────────────────
# Dependencias del backend (FastAPI + ML stack)
# PyTorch se instala por separado via conda en desarrollo local
# En Docker se usa la versión pip con CUDA si está disponible

# ── Framework web ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
aiofiles>=23.2.1

# ── Autenticación ─────────────────────────────────────────────────────────────
bcrypt>=4.0.1  
python-jose[cryptography]>=3.3.0

# ── LangChain ecosystem ───────────────────────────────────────────────────────
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-chroma>=0.1.0
langchain-text-splitters>=0.2.0
langchain-huggingface>=0.0.3
langgraph>=0.1.0
langsmith>=0.1.0

# ── LlamaIndex ────────────────────────────────────────────────────────────────
llama-index-core>=0.10.0
llama-index-readers-file>=0.1.0
llama-index-retrievers-bm25>=0.1.0
# llama-index-node-parser-sentence>=0.1.0
llama-index-embeddings-huggingface>=0.2.0

# ── Vector store ──────────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── ML / Embeddings ───────────────────────────────────────────────────────────
# PyTorch: en Docker usa CPU por defecto
# Para CUDA en Docker se necesita imagen base nvidia/cuda (ver nota abajo)
torch>=2.1.0
sentence-transformers>=2.7.0
transformers>=4.40.0
accelerate>=0.27.0
huggingface-hub>=0.22.0
safetensors>=0.4.0

# ── Procesamiento de documentos ───────────────────────────────────────────────
pypdf>=4.0.0
pdfplumber>=0.10.0
python-docx>=1.1.0
ebooklib>=0.18.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
openpyxl>=3.1.0

# ── Generación de documentos ──────────────────────────────────────────────────
reportlab>=4.1.0
latex2mathml>=3.77.0


# ── Búsqueda web ──────────────────────────────────────────────────────────────
ddgs>=6.0.0
requests>=2.31.0

# ── Ciencia de datos ──────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0
matplotlib>=3.8.0
sympy>=1.12.0
scikit-learn>=1.4.0

# ── Utilidades ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
httpx>=0.27.0
