# ── requirements.frontend.txt — Frontend ──────────────────────────────────────
# Solo las dependencias que necesita Streamlit
# Imagen más ligera que el backend

streamlit>=1.35.0
requests>=2.31.0
python-dotenv>=1.0.0
